<footer class="bg-white text-center p-lg-5 p-2 text-dark mb-0">
    <div class="container p-4">
        <div class="row justify-content-md-center">
            <div class="col-md-5 col-lg-3 col-lg-3 mb-md-0 mx-md-0 mx-auto mb-4">
                <img src="{{ asset('build/images/logo_final.png') }}" alt="logo" class="logo-footer">
            </div>
            <div class="col-md-3 mb-4 mb-md-0">
                <div class="mb-0 mt-md-0 mt-2 d-flex justify-content-md-center">
                    <div class="mb-0 d-flex flex-column justify-content-start align-items-start text-decoration-none">
                        <h2 class="mb-4 title">Liens</h2>
                        <a class="mb-2" href="{{ path('home') }}">Accueil</a>
                        <a class="mb-2" href="{{ path('home', {'_fragment': 'presentation'}) }}">A propos</a>
                        {% if app.user %}
                            <a class="mb-2" href="{{ path('permutsearch') }}">Permut Search</a>
                            <a class="mb-2" href="{{ path('profile_edit', {username: app.user.username}) }}">Mon profil</a>
                            {% if app.user and app.user.registeredUser and app.user.registeredUser.subscription %}
                                <a class="mb-2" href="{{ path('profile_showLike', {username: app.user.username}) }}">Mes likes</a>
                                <a class="mb-2" href="{{ path('match') }}">Mes matchs</a>
                            {% endif %}
                            <a class="mb-2" href="{{ path('setting', {username: app.user.username}) }}">Paramètres</a>
                        {% endif %}
                        {% if app.user is empty %}
                            <a class="mb-2" data-bs-toggle="modal" data-bs-target="#loginModal" href="#">
                                Connexion
                            </a>
                            <a class="mb-2" href="{{ path('app_register') }}">Inscription</a>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-0">
                <div class="mb-0 d-flex flex-column align-items-start">
                    <h2 class="mb-4 title text-nowrap">Contactez nous</h2>
                    <p class="mb-2 paragraph">11 Rue d’Illiers</p>
                    <p class="mb-2 paragraph">45000 Orléans</p>
                    <p class="mb-2 paragraph">07 49 62 53 30</p>
                    <p class="mb-2 paragraph">mario@jobpermut.fr</p>
                </div>
                <div class="my-3 d-flex justify-content-start">
                    <a class="me-2" href="#" role="button">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a class="me-2" href="https://www.facebook.com/jobpermut" target="_blank" role="button">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a class="me-2" href="#" role="button">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a class="me-2" href="#" role="button">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                </div>
            </div>
        </div>
        <p class="mt-5 fw-light text-primary">
            Ce service utilise l'API Rome Pôle-Emploi.
        </p>
    </div>
</footer>
