<form method="post" action="{{ path('testimony_delete', {'id': testimony.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimony.id) }}">
    <button class="btn btn-danger m-2">Supprimer</button>
</form>
