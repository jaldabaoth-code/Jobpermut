{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning m-2 mx-0 w-100">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
